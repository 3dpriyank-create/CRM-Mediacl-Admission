# Integrations
Provider-specific implementations are exposed from dedicated modules: `email`, `whatsapp`, `payment`, `storage`, and `notifications`. This folder is reserved for shared provider contracts and cross-provider orchestration.
